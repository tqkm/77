class Solution {
    public void reverseString(char[] s) {
        int p1 = 0;
        int p2 = s.length - 1;
        char tmp;
        while(p1 < p2){
            tmp = s[p1];
            s[p1] = s[p2];
            s[p2] = tmp;
            p1++;
            p2--;
        }
    }
}
